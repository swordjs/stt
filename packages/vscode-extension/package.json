{
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "main": "./lib/index.cjs",
  "files": [
    "lib"
  ],
  "engines": {
    "vscode": "^1.69.0"
  },
  "activationEvents": [
    "onStartupFinished"
  ],
  "scripts": {
    "dev": "nodemon --watch src/ -C -e ts --debug -x 'npm run build'",
    "build": "unbuild"
  },
  "devDependencies": {
    "@types/vscode": "^1.69.0"
  }
}
